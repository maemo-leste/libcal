on: [push]

jobs:
  call-workflow:
    strategy:
      matrix:
        - os: docker
          release: daedalus
        - os: docker-arm64
          release: daedalus
    runs-on: [docker, docker-arm64]
    container:
      image: docker.io/devuan/devuan:${{ matrix.release }}
    name: Matrix job
    uses: leste/forgejo-workflows/.forgejo/workflows/maemo-entry.yml@master
